/**
 * Different stages inside the game intro.
 * Required so the game intro knows which stage it is in and therefore decide what to render.
 */
public enum GAME_INTRO_STAGE {
    SPAWNING,
    MOVING_UP,
    TITLE,
    POINT_TABLE,
    SCORE_RANKING
}
